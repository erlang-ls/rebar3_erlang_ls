{erl_opts, [debug_info]}.
{deps, [ {meck, "0.9.0"} ]}.

{shell, [
  % {config, "config/sys.config"},
    {apps, [sample]}
]}.

%% TODO: Windows variant
{pre_hooks, [ {compile, "rm -rf _build/default/plugins/rebar3_bsp"}
            , {compile, "mkdir -p _build/default/plugins"}
            , {compile, "ln -sf $(pwd)/../../ _build/default/plugins/rebar3_bsp"}
            ]
}.
{project_plugins, [ %% The rebar3_bsp will be symlinked, so just use a placeholder here
                    {rebar3_bsp, {git, "dummy", {branch, "dummy"}}}
                  ]}.
